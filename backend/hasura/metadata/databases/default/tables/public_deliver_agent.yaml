table:
  name: deliver_agent
  schema: public
array_relationships:
  - name: in_travels
    using:
      foreign_key_constraint_on:
        column: d_id
        table:
          name: in_travel
          schema: public
